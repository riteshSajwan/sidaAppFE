import React from 'react'
import { ScrollView, Text } from 'react-native'

const NewApplicationContainer = () => {
  return (
    <ScrollView>
      <Text>NewApplicationContainer</Text>
    </ScrollView>
  )
}

export default NewApplicationContainer